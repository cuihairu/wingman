# 架构设计

## 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Wingman 架构                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │              Network Layer (C++)                          │   │
│   │                                                          │   │
│   │   ┌──────────────┐        ┌──────────────┐             │   │
│   │   │ TCP Server   │        │ TCP Client   │             │   │
│   │   │ (被动模式)    │        │ (主动连接)    │             │   │
│   │   └──────────────┘        └──────────────┘             │   │
│   │                                                          │   │
│   └──────────────────────────────┬───────────────────────────┘   │
│                                  │                               │
│   ┌──────────────────────────────┴───────────────────────────┐   │
│   │                    Core Engine (C++)                     │   │
│   │                                                          │   │
│   │   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────┐  │   │
│   │   │  Screen  │  │  Input   │  │  Window  │  │Process │  │   │
│   │   └──────────┘  └──────────┘  └──────────┘  └───────┘  │   │
│   │                                                          │   │
│   │   ┌──────────┐  ┌──────────┐  ┌──────────┐             │   │
│   │   │  Human   │  │   Macro  │  │ Trigger  │             │   │
│   │   └──────────┘  └──────────┘  └──────────┘             │   │
│   │                                                          │   │
│   └──────────────────────────────┬───────────────────────────┘   │
│                                  │                               │
│   ┌──────────────────────────────┴───────────────────────────┐   │
│   │                  Lua Script Engine                      │   │
│   │                                                          │   │
│   │   ┌─────────────────────────────────────────────────┐    │   │
│   │   │              LuaJIT 2.1                          │    │   │
│   │   │                                                    │    │   │
│   │   │   - 脚本解析和执行                                  │    │   │
│   │   │   - JIT 编译                                      │    │   │
│   │   │   - C++ API 绑定                                  │    │   │
│   │   │   - 调试 Hook                                     │    │   │
│   │   └─────────────────────────────────────────────────┘    │   │
│   │                                                          │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 模块设计

### 核心模块

| 模块 | 职责 | 文件 |
|-----|------|------|
| **Screen** | 屏幕操作 | `screen.cpp/h` |
| **Input** | 输入模拟 | `input.cpp/h` |
| **Window** | 窗口管理 | `window.cpp/h` |
| **Process** | 进程管理 | `process.cpp/h` |
| **Human** | 人性化模拟 | `human.cpp/h` |
| **Macro** | 宏录制 | `macro.cpp/h` |
| **Trigger** | 触发器系统 | `trigger.cpp/h` |
| **Network** | 网络层 | `network.cpp/h` |
| **Debugger** | 调试器 | `debugger.cpp/h` |
| **Utils** | 工具函数 | `utils.cpp/h` |

## C++ / Lua 分层

```
┌─────────────────────────────────────────────────────────────────┐
│                    分层决策                                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   C++ 实现                    Lua 实现                         │
│   ────────                    ─────────                         │
│                                                                  │
│   ├── 性能敏感操作                ├── 业务逻辑                  │
│   ├── 系统调用                    ├── 状态机                    │
│   ├── 内存操作                    ├── 触发器组合                │
│   ├── 图像处理                    ├── 用户自定义行为            │
│   └── └─ Windows API              └── └─ 脚本编排               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 目录结构

\`\`\`
wingman/
├── .github/
│   └── workflows/           # CI/CD 配置
│       ├── build.yml
│       └── release.yml
├── docs/                    # VitePress 文档
│   ├── .vitepress/
│   ├── guide/
│   ├── api/
│   └── examples/
├── server/                  # 网络服务层
│   ├── include/
│   └── src/
├── core/                    # C++ 核心引擎
│   ├── include/
│   └── src/
├── debugger/                # 调试器
│   ├── include/
│   └── src/
├── bindings/                # Lua 绑定
│   └── sol2/
├── scripts/                 # Lua 脚本
│   ├── examples/
│   └── api/
├── tools/                   # 开发工具
│   └── debugger_client/
├── tests/                   # 测试
│   ├── unit/
│   └── integration/
├── CMakeLists.txt
├── vcpkg.json
└── README.md
\`\`\`

## 数据流

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│   Nebula / CLI               Wingman Core                        │
│      │                            │                             │
│      │ 1. 发送指令                 │                             │
│      ├────────────────────────────▶│                             │
│      │                            │                             │
│      │                            │ 2. 解析指令                 │
│      │                            ├──────────────┐              │
│      │                            │              │              │
│      │                            │              ▼              │
│      │                            │         Lua Engine        │
│      │                            │              │              │
│      │                            │ 3. 执行脚本  │              │
│      │                            │◀─────────────┤              │
│      │                            │              │              │
│      │                            │ 4. 调用 API  │              │
│      │                            ├──────────────┤              │
│      │                            │              │              │
│      │                            │              ▼              │
│      │                            │         C++ Core         │
│      │                            │  (Screen/Input/...)     │
│      │                            │              │              │
│      │                            │ 5. 执行操作  │              │
│      │                            ├──────────────┤              │
│      │                            │              │              │
│      │                            │              ▼              │
│      │                            │         Windows          │
│      │                            │              │              │
│      │ 6. 返回结果                 │                             │
│      │◀────────────────────────────┤                             │
│      │                            │                             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 线程模型

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│   主线程                     网络线程                        │
│   ──────                     ────────                        │
│                                                                  │
│   ┌─────────────┐           ┌─────────────┐                    │
│   │ Lua Engine  │           │ TCP Server  │                    │
│   │             │           │ / Client    │                    │
│   │ 脚本执行     │           │             │                    │
│   │             │           │ 接收指令     │                    │
│   │ API 调用     │           │ 发送结果     │                    │
│   │             │           │             │                    │
│   └─────────────┘           └─────────────┘                    │
│         │                         │                             │
│         │ 消息队列                │                             │
│         ▼                         │                             │
│   ┌─────────────┐                  │                             │
│   │  C++ Core   │                  │                             │
│   │             │                  │                             │
│   │ Screen/Input │                  │                             │
│   │ Window/Process│                 │                             │
│   │             │                  │                             │
│   └─────────────┘                  │                             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```
